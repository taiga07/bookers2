<%= render 'shared/template', book: @book, user: @user %>


<h2>Books</h2>

<table>

  <thed>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thed>

  <tbody>
  <% @books.each do |book| %>
    <tr>
      <td><%= book.user_id %></td>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
    </tr>
  <%end%>
  </tbody>

</table>